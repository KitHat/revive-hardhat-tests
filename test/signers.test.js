const { ethers } = require("hardhat");
const { expect } = require('chai');

// describe("signers", async function () {
//     it("should be funded", async function () {
//         let [first, second] = await ethers.getSigners();
//         expect(await ethers.provider.getBalance(first.address)).to.not.equal(0);
//         expect(await ethers.provider.getBalance(second.address)).to.not.equal(0);
//     })
// })